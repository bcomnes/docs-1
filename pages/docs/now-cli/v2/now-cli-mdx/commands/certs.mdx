import Snippet from '~/components/snippet'
import Caption from '~/components/text/caption'
import { InlineCode } from '~/components/text/code'
import Note from '~/components/text/note'

export const meta = {
  editUrl: 'pages/docs/now-cli/v2/now-cli-mdx/commands/certs.mdx',
  lastEdited: '2019-09-03T23:06:27.000Z'
}

## Certs

The `now certs` command is used to manage certificates for domains, providing functionality to list, issue and remove them.

<Note type="warning">
  This command is intended for <b>advanced use only</b>. By default, ZEIT Now
  manages your certificates automatically.
</Note>

### Basic Usage

<Snippet dark text="now certs ls" />
<Caption>Using the <InlineCode>now certs</InlineCode> command to list all certificates under an account.</Caption>

### Extended Usage

<Snippet dark text="now certs issue [domain1, domain2, domain3]" />
<Caption>Using the <InlineCode>now certs</InlineCode> command to issue certificates for multiple domains.</Caption>

<Snippet dark text="now certs rm [certificate-id]" />
<Caption>Using the <InlineCode>now certs</InlineCode> command to remove a certificate by ID.</Caption>

### Global Options

The following [global options](#options) can be passed when using the `now certs` command:

- `--debug`
- `--force`
- `--global-config`
- `--help`
- `--local-config`
- `--scope`
- `--token`

For more information on global options and their usage, refer to the [options section](#options).

### Unique Options

These are options that only apply to the `now certs` command, therefore, more information is provided.

#### Challenge Only

The `--challenge-only` option can be used to only show the challenges needed to issue a certificate.

##### Usage Example

<Snippet dark text="now certs issue foo.com --challenge-only" />
<Caption>Using the <InlineCode>now certs</InlineCode> command with the <InlineCode>--challenge-only</InlineCode> option.</Caption>

#### Challenge Only

The `--challenge-only` option can be used to only show the challenges needed to issue a certificate.

##### Usage Example

<Snippet dark text="now certs issue foo.com --challenge-only" />
<Caption>Using the <InlineCode>now certs</InlineCode> command with the <InlineCode>--challenge-only</InlineCode> option.</Caption>

#### CRT

The `--crt` option can be used to upload a certificate file for a domain by providing a path to it.

##### Usage Example

<Snippet dark text="now certs issue foo.com --crt foo.crt" />
<Caption>Using the <InlineCode>now certs</InlineCode> command with the <InlineCode>--crt</InlineCode> option.</Caption>

#### Key

The `--key` option can be used to upload a certificate key file for a domain by providing a path to it.

##### Usage Example

<Snippet dark text="now certs issue foo.com --key foo.pem" />
<Caption>Using the <InlineCode>now certs</InlineCode> command with the <InlineCode>--key</InlineCode> option.</Caption>

#### CA

The `--ca` option can be used to upload a certificate chain file for a domain by providing a path to it.

##### Usage Example

<Snippet dark text="now certs issue foo.com --ca foo.ca" />
<Caption>Using the <InlineCode>now certs</InlineCode> command with the <InlineCode>--ca</InlineCode> option.</Caption>
